import * as api from "../api";

export const signIn = (form, history) => {
  try {
    //sign in..
  } catch (error) {
    console.log(error);
  }
};

export const signUp = (form, history) => {
  try {
    //sign Up..
  } catch (error) {
    console.log(error);
  }
};
